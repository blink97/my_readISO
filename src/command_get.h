#ifndef COMMAND_GET_H
# define COMMAND_GET_H

# include <stdio.h>
# include <string.h>
# include "useful_func.h"

void cmd_get(struct iso_dir *root, void *ptr, char *arg);

#endif /* !COMMAND_GET_H */
